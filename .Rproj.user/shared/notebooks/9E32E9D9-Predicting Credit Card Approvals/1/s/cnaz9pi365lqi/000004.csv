"0","```r

# Descartando as variaveis 12 e 14
cc_apps = cc_apps %>% select(-X12, -X14)

# Substituindo os '?'s por NA
cc_apps = cc_apps %>% mutate_all(~ifelse(. == \"?\", NA, .))

cc_apps = cc_apps %>% 
  mutate(X2 = as.numeric(X2)) %>%
  mutate_if(~is.character(.), ~as.factor(.)) %>%
  mutate_if(~is.logical(.), ~as.factor(.))
```"
